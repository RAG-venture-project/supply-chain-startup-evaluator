"""0. 스타트업 선택 라우터 (단순 함수 — LLM 없음)."""

from src.schemas.state import GraphState


def select_startup(state: GraphState) -> GraphState:
    """다음 평가 대상 스타트업을 꺼내서 state에 세팅한다."""
    # TODO: 구현
    ...
